#include "stdafx.h"
#include <iostream>
#include "conio.h"
#include <string>
using namespace std;
void mayuscula(string &frase);
void main()
{
string frase;
cout<<"escriba la frase en minuscula: ";
getline(cin,frase);
mayuscula(frase);
cout<<frase<<endl;
getch();
}
void mayuscula(string &frase){
for(int i=0;i<frase.length();i++)
	frase[i]=frase[i]-32;
}
